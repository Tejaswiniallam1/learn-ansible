- name: Just say hello
  hosts: all
  tasks:
    - name: Say hello
      ansible.builtin.debug:
      msg: hello

- name: Just say bye
  hosts: all
  tasks:
    - name: say bye
      ansible.builtin.debug:
      msg: bye